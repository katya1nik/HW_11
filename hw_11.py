"""
Задание 📃
**Разработка итератора для обработки данных о городах России**

## Краткое содержание 
В данном задании вам необходимо создать датакласс `City` и класс-итератор `CitiesIterator`. Итератор должен принимать список словарей с информацией о городах, преобразовывать каждый словарь в экземпляр `City` (с обработкой вложенного словаря `coords` для полей `lat` и `lon`), а также обеспечивать валидацию данных, фильтрацию по населению и возможность сортировки городов по различным параметрам.

### Технологии: 🦾
- Python 3.x
- Модуль dataclasses (`@dataclass`)
- Итераторы и генераторы
- Валидация данных
- Основы объектно-ориентированного программирования

## Задание 👷‍♂️

### Описание задания и требования

1. Создайте датакласс `City` с полями:
   - `name` (название города)
   - `lat` (широта)
   - `lon` (долгота)
   - `district` (федеральный округ)
   - `population` (население)
   - `subject` (субъект РФ)

"""
from dataclasses import dataclass
@dataclass
class City:
   name: str
   lat: float
   lon: float
   district: str
   population: int
   subject: str


"""
2. Реализуйте класс-итератор `CitiesIterator` со следующим функционалом:
   - **Конструктор:** принимает список словарей с информацией о городах. Каждый словарь включает обязательные поля: `name`, `district`, `population`, `subject` и вложенный словарь `coords` с полями `lat` и `lon`.
   - **Итерация:** при вызове итератора каждый словарь преобразуется в экземпляр `City`. При этом значения для `lat` и `lon` извлекаются из вложенного словаря `coords`.
   - **Валидация данных:** на этапе инициализации или итерации необходимо проверить, что все необходимые поля присутствуют в исходном словаре. Если некоторые поля отсутствуют, выбросить соответствующее исключение с информативным сообщением.
   - **Фильтрация по населению:** добавьте метод `set_population_filter(min_population: int)` для установки минимального порога населения. Итератор должен возвращать только те города, у которых значение `population` не меньше заданного.
   - **Сортировка:** реализуйте возможность сортировки списка городов по заданному параметру. Это может быть метод (например, `sort_by(parameter: str, reverse: bool = False)`) или параметр конструктора, который задаёт критерий сортировки.

>[!info]
>#### Основные этапы выполнения задания
>- Создание датакласса `City` с указанными полями.
>- Реализация конструктора класса `CitiesIterator`, принимающего список словарей.
>- При итерации: проверка наличия всех необходимых ключей, извлечение координат из поля `coords` и создание экземпляров `City`.
>- Добавление метода `set_population_filter` для фильтрации городов по населению.
>- Реализация сортировки городов по заданному параметру.

### Примеры использования
Предположим, у вас есть список городов:
```python
cities_list = [
    {
        "coords": {
            "lat": "52.65",
            "lon": "90.08333"
        },
        "district": "Сибирский",
        "name": "Абаза",
        "population": 14816,
        "subject": "Хакасия"
    },
    # Другие записи ...
]
```
Пример работы с итератором:
```python
cities_iterator = CitiesIterator(cities_list)
cities_iterator.set_population_filter(15000)  # Фильтрация по минимальному населению 15000
cities_iterator.sort_by("name")               # Сортировка по названию города
for city in cities_iterator:
    print(city)
```
В данном примере задолженность обработки подразумевает, что только города с населением не меньше 15000 будут выведены, а порядок будет отсортирован по названию.

>[!warning]
>#### Важные моменты ⚠️
>- Проверка наличия полей: `name`, `district`, `population`, `subject` и `coords` (внутри которого должны быть `lat` и `lon`).
>- При отсутствии обязательного поля выбрасывайте исключение с понятным сообщением.
>- Фильтрация и сортировка должны корректно обновлять состояние итератора, не нарушая возможности последовательного вызова `__iter__()`.

### Таблица классов и методов

| Класс             | Файл        | Методы и функции                                 | Описание                                                                                     | Использование                                   |
| ----------------- | ----------- | ------------------------------------------------ | -------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| `City`            | `iterator.py` | Параметры: `name`, `lat`, `lon`, `district`, `population`, `subject`  | Датакласс, представляющий данные о городе с выделенными полями координат.                      | Создание экземпляров города                     |
| `CitiesIterator`  | `iterator.py` | `__init__(cities: list[dict])`                   | Инициализация итератора списком словарей, содержащих данные городов.                          | Прием исходных данных                           |
|                   |             | `__iter__()`                                   | Начало итерации; возвращает итератор, возможно, после фильтрации/сортировки.                  | Реализация протокола итератора                  |
|                   |             | `__next__()`                                   | Возвращает следующий экземпляр `City`, преобразованный из словаря и проверенный на валидность. | Итерация по городам                             |
|                   |             | `set_population_filter(min_population: int)`   | Устанавливает минимальное значение населения для фильтрации городов.                         | Фильтрация по населению                         |
|                   |             | `sort_by(parameter: str, reverse: bool = False)` | Сортирует внутренний список городов по значению указанного поля.                              | Сортировка по выбранному параметру              |

## Дополнительные указания

1. **Аннотация типов и документация:**
   - Во всех классах и методах используйте аннотации типов для параметров и возвращаемых значений.
   - Каждый метод должен иметь подробный комментарий (docstring). Опишите назначение метода, входные параметры и возвращаемые данные.

2. **Валидация и обработка ошибок:**
   - При преобразовании словаря в объект `City` проверьте, что все необходимые ключи присутствуют.
   - Если обнаружены несоответствия – выбрасывайте исключения с понятными сообщениями об ошибке.
   
3. **Управление состоянием итератора:**
   - После установки фильтрации или сортировки итератор должен корректно обновлять порядок возвращаемых элементов.
   - Обеспечьте возможность повторного обхода итератора (например, реализуйте метод `__iter__()` таким образом, чтобы можно было начать новую итерацию).

>[!warning]
>#### Критерии проверки 👌
>- Корректное создание датакласса `City` с требуемыми полями.
>- Правильная реализация конструктора и методов `__iter__()` и `__next__()` в классе `CitiesIterator`.
>- Валидация входящих данных: проверка обязательных ключей и корректная обработка ошибки при их отсутствии.
>- Реализация метода `set_population_filter` для фильтрации по населению.
>- Возможность сортировки городов по различным параметрам через метод `sort_by`.
>- Код должен соответствовать стандарту PEP-8 и включать подробные аннотации типов и docstring.
>- Приведенный пример использования должен корректно работать и демонстрировать функциональность итератора.
"""